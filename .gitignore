node_modules
*.DS_Store
dist
coverage
**/.env
**/.env.local
**/.env.*.local
*.log
*.history
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Optional: Ignore build artifacts
build

# Cypress artifacts
cypress/downloads
cypress/screenshots
cypress/videos

# Playwright MCP scratch snapshots
.playwright-mcp/

# Stray server-side dev DB (canonical lives at data/pizzamath.db)
server/data/*.db
server/data/*.db-shm
server/data/*.db-wal

# Ad-hoc verification screenshots at project root
/grading-result-verify.png

# Runtime-stored user upload images (created by the app/e2e at runtime)
/uploads/

# Git worktrees for parallel feature development
.worktrees/
