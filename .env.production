# GitHub Pages build must talk to the real backend, not MSW mocks —
# otherwise uploads/gradings/etc. live only in-memory and vanish on reload.
VITE_USE_MOCK=false
VITE_API_BASE_URL=https://pizzamath.onrender.com
